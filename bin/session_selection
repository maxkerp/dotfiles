# Show a selectable list of all tmux session

tmux list-sessions \
  | sed -E 's/:.*$//' \
  | grep -v "^$(tmux display-message -p '#S')\$" \
  | fzf --height=20% --color=$(CTP_THEME=frappe fzf_default_color_opts) --reverse \
  | xargs tmux switch-client -t 2> /dev/null
